{
  "scripts": {
    "build-css": "tailwindcss build style.css -o output.css",
    "watch-css": "tailwindcss build -i style.css -o output.css --watch",
    "dev": "concurrently \"npm run watch-css\" \"npm start\""
  },
  "dependencies": {
    "@tailwindcss/line-clamp": "^0.4.4",
    "animate.css": "^4.1.1",
    "swiper": "^11.0.7",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "postcss-import": "^16.0.1"
  }
}
